# Xcode
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
build/
*.xcuserstate

# macOS
.DS_Store

# Stale tooling artifacts
firebase-debug.log
firebase-debug.*.log

# Local Claude Code state (kept out of the app repo)
.claude/

# Plans / session artifacts (live in ~/.claude/plans, not in the repo)
plans/
tasks/
